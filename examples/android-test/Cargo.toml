[package]
name = "maplibre-android-test"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
maplibre_native = { workspace = true, features = ["opengl"] }

[lints]
workspace = true
